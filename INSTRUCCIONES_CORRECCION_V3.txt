LINOEM DEVELOPMENT — CORRECCIÓN V3 WINDOWS

DESCARGA SOLAMENTE EL ZIP COMPLETO.

Después de descomprimirlo, ejecuta únicamente:

EJECUTAR_ACTUALIZADOR_GITHUB_V3.bat

NO ejecutes directamente el archivo Python.

ERROR CORREGIDO:
El actualizador V2 intentaba ejecutar npm.cmd con comillas incorrectas:
'\"C:\Program Files\nodejs\npm.cmd\"' is not recognized.

La versión V3 ejecuta directamente:

node.exe node_modules\npm\bin\npm-cli.js

Esto evita el problema de npm.cmd y las rutas con espacios.

La versión V3:
- conserva package-lock.json;
- usa npm ci;
- valida Prisma, ESLint, TypeScript, pruebas y Next.js;
- solo actualiza GitHub si la validación termina correctamente;
- conserva respaldo local y remoto;
- prepara Railway con Node 22 Bookworm Slim.
